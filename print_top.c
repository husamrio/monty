#include "monty.h"
/**
 * print_top - prints the top of the stack
 * @stack_head: stack head of th prog
 * @counter: line no
 * Return: None.
*/
void print_top(stack_t **stack_head, unsigned int counter)
{
	(void)stack_head;
	(void)counter;
	bus.lifi = 0;
}
